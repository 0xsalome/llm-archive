# AGENTS.md - LLM書庫

## 基本方針

このリポジトリは、LLMとともに書かれた観察、仮説、断片、整理中の知識を置いておくための小さな書庫です。

完成した知識だけを置く場所ではありません。未完成であること、不確かであること、あとで見直す必要があることを隠さずに残します。

AI Agentは下書き、要約、構造化、リンク候補の提案を支援します。人間がテーマ設定、編集判断、公開可否、`status` の宣言に責任を持ちます。

## 依存関係ポリシー

- このプロジェクトは `pnpm` を使います。
- `pnpm-workspace.yaml` で `minimumReleaseAge: 4320` を設定し、新しい依存バージョンを3日遅延します。
- AIは `package.json`、依存関係、GitHub Actions、デプロイ設定を変更しません。

## ブランチ運用

- 作業開始時に現在のブランチを確認する
- 記事を書く・編集する場合は、ユーザーの明示がなくても、作業前に用途が分かる新規ブランチを切る
- すでに記事執筆用のブランチにいる場合は、勝手に再利用せず、続けるか `main` へ戻るか確認する
- 記事以外の基本的な変更では、ユーザーが明示した場合だけ新規ブランチを切る
- PRがマージ済みの作業ブランチには残らず、次の作業前に `main` へ戻る
- `git push` する前に、push先のブランチ名を確認する

## AIが行ってよいこと

- `src/content/docs/archive/` 以下の記事作成・編集
- frontmatter の基本項目と軽量メタデータの整理
- タグ、カテゴリ、関連ノート、内部リンク候補の提案
- 誤字脱字の修正
- 要約、比較、構造化、批評、リライト
- 古いノートとの矛盾候補の指摘
- README、AGENTS、記事テンプレート、公開前チェックリストの改善

## AIが行わないこと

- `package.json` や依存関係の変更
- GitHub Actions やデプロイ設定の変更
- 外部APIキー、個人情報、非公開情報の記述
- 出典を確認していない内容の強い断定
- 既存ノートの黙った上書き
- 人間の確認なしに `main` ブランチへ直接反映すること

## 事実先行の執筆ルール

記事を書く・編集するときは、事実・出来事・観察を先に書き、抽象的な評価や概念化はその後に置きます。

悪い例:

> この資料は固定された標本ではなく、更新される比較単位である。

改善例:

> 著者は後から届いた異文を追記し、それに合わせて以前の解釈を修正した。

- 対立する概念を立てる前に、誰が・何を・どうしたかを書く
- 次の言い回しを続けて使わない
  - 「〜ではなく、〜である」
  - 「〜とは限らない」
  - 「〜を意味しない」
  - 「重要なのは〜である」
- 段落ごとに新しい情報を入れ、前の段落の結論を言い換えない
- 「重要である」「興味深い」などの評価語には、根拠となる事実を添える
- 結論を変える留保だけを、関係する主張の近くに一度だけ置く
- 確認できない点は、何が、なぜ確認できないかを具体的に書く
- 逆接は、それがないと読者が誤解する場合に使う
- 概念化は、根拠となる事実のまとまりごとに一度に絞る

出力前に、次の3点を内部で確認します。見直しの過程は出力しません。

1. 否定してから言い直す文を、具体的な事実や行為に置き換えられないか
2. 各段落に新しい情報があるか
3. 根拠のない評価語が残っていないか

## 作業前に読む文書

作業内容に応じて、該当する文書を先に読みます。

- 記事を書く・編集する場合: `docs/agent-writing-guide.md` と `docs/frontmatter-guide.md` を必ず読む
- 記事に画像を入れる場合: `docs/research-article-guide.md` の「画像利用条件」を必ず読む
- Webリサーチ記事を書く場合: `docs/agent-writing-guide.md`、`docs/frontmatter-guide.md`、`docs/research-article-guide.md` を必ず読む
- frontmatterやタグを直す場合: `docs/frontmatter-guide.md` を読む
- JP/EN切替を実装する場合: `docs/i18n-implementation-note.md` を読む
- カテゴリや内部リンクを整理する場合: `docs/category-and-linking-guide.md` も参照する
- 公開前確認をする場合: `docs/publishing-checklist.md` も参照する

この記事執筆系ルールは `docs/` 以下にも分離しているため、該当作業前に必ず参照します。記事を書く・編集する場合は、この `AGENTS.md` の「事実先行の執筆ルール」も必ず適用します。
